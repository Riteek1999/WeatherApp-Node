<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="stylesheets/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/aa87c9f1f1.js" crossorigin="anonymous"></script>
    
</head>
<body>
    <div class="container">
        <header>
            <i class="fas fa-cloud-sun fa-5x"></i>
        </header>

        <div class="weather-title">
            <h1>Weather App</h1>
            <p>View the current weather of your city</p>
        </div>
        
        <form action="/" method="POST">
            <input type="text" name="city" placeholder="Enter your city name" required>
            <button type="submit" class="btn btn-info btn3d"><i class="fas fa-cloud-upload-alt"></i> Get</button>
        </form>

        <div class="weather">
            <div>
                <% if(temp!==null) { %>
                <h2> <%=temp%><span>&#8451;</span></h2>
                <% } %>
                <div class="description">
                    <% if(icon!==null) { %>
                    <img class="icon" src="http://openweathermap.org/img/w/<%=icon%>.png">
                    <% } %>
                    <% if(desc!==null) { %>
                        <h3> <%=desc%></h3>
                    <% } %>
                </div>
                <% if(city!==null && city!=='notfound') { %>
                    <h2> <%=city%></h2>
                <% } %>
                <% if(city=='notfound') { %>
                    <h5>City not found try any other city </h5>
                <% } %>
            </div>
        </div>
    </div>
    
    
</body>
</html>